<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="20" skipped="0" tests="20" time="145.790" timestamp="2025-12-03T16:55:11.927061+00:00" hostname="ip-10-2-3-62"><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-HEAVY-01[default|t3]]" time="3.540"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/.env --project-name ubuntutestschaheavy01defaultt3local6052fc2c-gw20 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestschaheavy01defaultt3local6052fc2c-gw20_default  Creating&#10; Network ubuntutestschaheavy01defaultt3local6052fc2c-gw20_default  Error&#10;failed to create network ubuntutestschaheavy01defaultt3local6052fc2c-gw20_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'default', 'faults': [{'kind': 'leader_kill_measure', True: 'leader', 'timeout_s': 60}, {'kind': 'parallel...atio': 0.1, 'type': 'error_rate_le'}, {'max_ms': 10000, 'type': 'p99_le'}, ...], 'id': 'CHA-HEAVY-01[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xedb051c36a20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-HEAVY-01[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/.env', '--project-name', 'ubuntutestschaheavy01defaultt3local6052fc2c-gw20', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/.env --project-name ubuntutestschaheavy01defaultt3local6052fc2c-gw20 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[default|t3]-local-6052fc2c-gw20/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestschaheavy01defaultt3local6052fc2c-gw20_default  Creating
E                Network ubuntutestschaheavy01defaultt3local6052fc2c-gw20_default  Error
E               failed to create network ubuntutestschaheavy01defaultt3local6052fc2c-gw20_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-04[rocksdb|t3]]" time="3.422"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/.env --project-name ubuntutestscha04rocksdbt3local2f3d1da1-gw11 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha04rocksdbt3local2f3d1da1-gw11_default  Creating&#10; Network ubuntutestscha04rocksdbt3local2f3d1da1-gw11_default  Error&#10;failed to create network ubuntutestscha04rocksdbt3local2f3d1da1-gw11_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'netem', True: 'all', 'loss_pct': 2, 'jitter_ms': 5, ...}], 'gates': [{'typ...ms': 4000, 'type': 'p95_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-04[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xeba24ee8ea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-04[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/.env', '--project-name', 'ubuntutestscha04rocksdbt3local2f3d1da1-gw11', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/.env --project-name ubuntutestscha04rocksdbt3local2f3d1da1-gw11 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[rocksdb|t3]-local-2f3d1da1-gw11/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha04rocksdbt3local2f3d1da1-gw11_default  Creating
E                Network ubuntutestscha04rocksdbt3local2f3d1da1-gw11_default  Error
E               failed to create network ubuntutestscha04rocksdbt3local2f3d1da1-gw11_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-06[rocksdb|t3]]" time="126.571"><failure message="AssertionError: cluster ready">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'parallel', 'steps': [{'kind': 'netem', True: 'all', 'delay_ms': 10, 'jitte...ms': 6000, 'type': 'p99_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-06[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xeaff39e4ea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-06[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:97: in _make
    wait_until(lambda: count_leaders(nodes) == 1, timeout_s=to, interval=0.5, desc="cluster ready")
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

cond = &lt;function cluster_factory.&lt;locals&gt;._make.&lt;locals&gt;.&lt;lambda&gt; at 0xeaff940837e0&gt;
timeout_s = 120.0, interval = 0.5, desc = 'cluster ready'

    def wait_until(cond, timeout_s=60.0, interval=0.5, desc=""):
        start = time.time()
        while True:
            try:
                if cond():
                    return True
            except Exception:
                pass
            if time.time() - start &gt; float(timeout_s):
&gt;               raise AssertionError(desc or "timeout")
E               AssertionError: cluster ready

tests/stress/keeper/framework/core/util.py:56: AssertionError</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-03[default|t3]]" time="126.820"><failure message="AssertionError: cluster ready">scenario = {'backend': 'default', 'faults': [{'kind': 'netem', True: 'all', 'delay_ms': 20, 'jitter_ms': 15, ...}], 'gates': [{'t...ms': 3000, 'type': 'p95_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-03[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xfa9e80a32a20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-03[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:97: in _make
    wait_until(lambda: count_leaders(nodes) == 1, timeout_s=to, interval=0.5, desc="cluster ready")
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

cond = &lt;function cluster_factory.&lt;locals&gt;._make.&lt;locals&gt;.&lt;lambda&gt; at 0xfa9e863f37e0&gt;
timeout_s = 120.0, interval = 0.5, desc = 'cluster ready'

    def wait_until(cond, timeout_s=60.0, interval=0.5, desc=""):
        start = time.time()
        while True:
            try:
                if cond():
                    return True
            except Exception:
                pass
            if time.time() - start &gt; float(timeout_s):
&gt;               raise AssertionError(desc or "timeout")
E               AssertionError: cluster ready

tests/stress/keeper/framework/core/util.py:56: AssertionError</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-HEAVY-01[rocksdb|t3]]" time="3.207"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/.env --project-name ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18_default  Creating&#10; Network ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18_default  Error&#10;failed to create network ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'leader_kill_measure', True: 'leader', 'timeout_s': 60}, {'kind': 'parallel...atio': 0.1, 'type': 'error_rate_le'}, {'max_ms': 10000, 'type': 'p99_le'}, ...], 'id': 'CHA-HEAVY-01[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xf61ef0a16a20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-HEAVY-01[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/.env', '--project-name', 'ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/.env --project-name ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-HEAVY-01[rocksdb|t3]-local-8fe2fb6c-gw18/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18_default  Creating
E                Network ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18_default  Error
E               failed to create network ubuntutestschaheavy01rocksdbt3local8fe2fb6c-gw18_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[RCFG-06[rocksdb|t3]]" time="3.368"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/.env --project-name ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7_default  Creating&#10; Network ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7_default  Error&#10;failed to create network ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'partition_symmetric_during', 'on': 'leader', 'steps': [{'kind': 'reconfig'...nfig_members_len_eq'}, {'type': 'backlog_drains'}, {'type': 'health_precheck'}, ...], 'id': 'RCFG-06[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xe76bd7336a20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[RCFG-06[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/.env', '--project-name', 'ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/.env --project-name ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[rocksdb|t3]-local-7563b1a8-gw7/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7_default  Creating
E                Network ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7_default  Error
E               failed to create network ubuntutestsrcfg06rocksdbt3local7563b1a8-gw7_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-02[rocksdb|t3]]" time="126.571"><failure message="AssertionError: cluster ready">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'netem', True: 'all', 'delay_ms': 25, 'jitter_ms': 7, ...}], 'gates': [{'ty...: 'log_sanity_ok'}, {'type': 'health_precheck'}, {'max_ms': 10000, 'type': 'p99_le'}], 'id': 'CHA-02[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xef554715ea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-02[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:97: in _make
    wait_until(lambda: count_leaders(nodes) == 1, timeout_s=to, interval=0.5, desc="cluster ready")
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

cond = &lt;function cluster_factory.&lt;locals&gt;._make.&lt;locals&gt;.&lt;lambda&gt; at 0xef554cb937e0&gt;
timeout_s = 120.0, interval = 0.5, desc = 'cluster ready'

    def wait_until(cond, timeout_s=60.0, interval=0.5, desc=""):
        start = time.time()
        while True:
            try:
                if cond():
                    return True
            except Exception:
                pass
            if time.time() - start &gt; float(timeout_s):
&gt;               raise AssertionError(desc or "timeout")
E               AssertionError: cluster ready

tests/stress/keeper/framework/core/util.py:56: AssertionError</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-01[rocksdb|t3]]" time="3.523"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/.env --project-name ubuntutestscha01rocksdbt3local53ea9d4c-gw0 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha01rocksdbt3local53ea9d4c-gw0_default  Creating&#10; Network ubuntutestscha01rocksdbt3local53ea9d4c-gw0_default  Error&#10;failed to create network ubuntutestscha01rocksdbt3local53ea9d4c-gw0_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'rocksdb', 'faults': [{'delay_ms': 10, 'duration_s': 180, 'jitter_ms': 5, 'kind': 'netem', ...}, {'duratio...ate_le'}, {'type': 'p99_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-01[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xe1dffa506a20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-01[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/.env', '--project-name', 'ubuntutestscha01rocksdbt3local53ea9d4c-gw0', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/.env --project-name ubuntutestscha01rocksdbt3local53ea9d4c-gw0 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[rocksdb|t3]-local-53ea9d4c-gw0/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha01rocksdbt3local53ea9d4c-gw0_default  Creating
E                Network ubuntutestscha01rocksdbt3local53ea9d4c-gw0_default  Error
E               failed to create network ubuntutestscha01rocksdbt3local53ea9d4c-gw0_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-10M-01[default|t3]]" time="127.667"><failure message="AssertionError: cluster ready">scenario = {'backend': 'default', 'faults': [{'kind': 'parallel', 'steps': [{'config': 'workloads/prod_mix.yaml', 'duration_s': 6..., 'Cannot read all data'], 'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-10M-01[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xf9ba6d15ea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-10M-01[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:97: in _make
    wait_until(lambda: count_leaders(nodes) == 1, timeout_s=to, interval=0.5, desc="cluster ready")
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

cond = &lt;function cluster_factory.&lt;locals&gt;._make.&lt;locals&gt;.&lt;lambda&gt; at 0xf9ba72b037e0&gt;
timeout_s = 120.0, interval = 0.5, desc = 'cluster ready'

    def wait_until(cond, timeout_s=60.0, interval=0.5, desc=""):
        start = time.time()
        while True:
            try:
                if cond():
                    return True
            except Exception:
                pass
            if time.time() - start &gt; float(timeout_s):
&gt;               raise AssertionError(desc or "timeout")
E               AssertionError: cluster ready

tests/stress/keeper/framework/core/util.py:56: AssertionError</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-02[default|t3]]" time="3.511"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/.env --project-name ubuntutestscha02defaultt3localaa318e23-gw4 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha02defaultt3localaa318e23-gw4_default  Creating&#10; Network ubuntutestscha02defaultt3localaa318e23-gw4_default  Error&#10;failed to create network ubuntutestscha02defaultt3localaa318e23-gw4_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'default', 'faults': [{'kind': 'netem', True: 'all', 'delay_ms': 25, 'jitter_ms': 7, ...}], 'gates': [{'ty...: 'log_sanity_ok'}, {'type': 'health_precheck'}, {'max_ms': 10000, 'type': 'p99_le'}], 'id': 'CHA-02[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xfb0cdd986a20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-02[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/.env', '--project-name', 'ubuntutestscha02defaultt3localaa318e23-gw4', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/.env --project-name ubuntutestscha02defaultt3localaa318e23-gw4 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-02[default|t3]-local-aa318e23-gw4/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha02defaultt3localaa318e23-gw4_default  Creating
E                Network ubuntutestscha02defaultt3localaa318e23-gw4_default  Error
E               failed to create network ubuntutestscha02defaultt3localaa318e23-gw4_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-06[default|t3]]" time="3.648"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/.env --project-name ubuntutestscha06defaultt3localf127f22a-gw15 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha06defaultt3localf127f22a-gw15_default  Creating&#10; Network ubuntutestscha06defaultt3localf127f22a-gw15_default  Error&#10;failed to create network ubuntutestscha06defaultt3localf127f22a-gw15_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'default', 'faults': [{'kind': 'parallel', 'steps': [{'kind': 'netem', True: 'all', 'delay_ms': 10, 'jitte...ms': 6000, 'type': 'p99_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-06[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xf3f52858aa20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-06[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/.env', '--project-name', 'ubuntutestscha06defaultt3localf127f22a-gw15', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/.env --project-name ubuntutestscha06defaultt3localf127f22a-gw15 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-06[default|t3]-local-f127f22a-gw15/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha06defaultt3localf127f22a-gw15_default  Creating
E                Network ubuntutestscha06defaultt3localf127f22a-gw15_default  Error
E               failed to create network ubuntutestscha06defaultt3localf127f22a-gw15_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-03[rocksdb|t3]]" time="3.444"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/.env --project-name ubuntutestscha03rocksdbt3locald90a8c45-gw9 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha03rocksdbt3locald90a8c45-gw9_default  Creating&#10; Network ubuntutestscha03rocksdbt3locald90a8c45-gw9_default  Error&#10;failed to create network ubuntutestscha03rocksdbt3locald90a8c45-gw9_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'netem', True: 'all', 'delay_ms': 20, 'jitter_ms': 15, ...}], 'gates': [{'t...ms': 3000, 'type': 'p95_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-03[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xe2e11f446a20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-03[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/.env', '--project-name', 'ubuntutestscha03rocksdbt3locald90a8c45-gw9', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/.env --project-name ubuntutestscha03rocksdbt3locald90a8c45-gw9 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-03[rocksdb|t3]-local-d90a8c45-gw9/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha03rocksdbt3locald90a8c45-gw9_default  Creating
E                Network ubuntutestscha03rocksdbt3locald90a8c45-gw9_default  Error
E               failed to create network ubuntutestscha03rocksdbt3locald90a8c45-gw9_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-10M-01[rocksdb|t3]]" time="3.162"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/.env --project-name ubuntutestscha10m01rocksdbt3local98195c62-gw14 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha10m01rocksdbt3local98195c62-gw14_default  Creating&#10; Network ubuntutestscha10m01rocksdbt3local98195c62-gw14_default  Error&#10;failed to create network ubuntutestscha10m01rocksdbt3local98195c62-gw14_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'parallel', 'steps': [{'config': 'workloads/prod_mix.yaml', 'duration_s': 6..., 'Cannot read all data'], 'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-10M-01[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xf57846a2ea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-10M-01[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/.env', '--project-name', 'ubuntutestscha10m01rocksdbt3local98195c62-gw14', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/.env --project-name ubuntutestscha10m01rocksdbt3local98195c62-gw14 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-10M-01[rocksdb|t3]-local-98195c62-gw14/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha10m01rocksdbt3local98195c62-gw14_default  Creating
E                Network ubuntutestscha10m01rocksdbt3local98195c62-gw14_default  Error
E               failed to create network ubuntutestscha10m01rocksdbt3local98195c62-gw14_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-01[default|t3]]" time="3.488"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/.env --project-name ubuntutestscha01defaultt3local44d7fcc9-gw1 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha01defaultt3local44d7fcc9-gw1_default  Creating&#10; Network ubuntutestscha01defaultt3local44d7fcc9-gw1_default  Error&#10;failed to create network ubuntutestscha01defaultt3local44d7fcc9-gw1_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'default', 'faults': [{'delay_ms': 10, 'duration_s': 180, 'jitter_ms': 5, 'kind': 'netem', ...}, {'duratio...ate_le'}, {'type': 'p99_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-01[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xf816bea2ea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-01[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/.env', '--project-name', 'ubuntutestscha01defaultt3local44d7fcc9-gw1', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/.env --project-name ubuntutestscha01defaultt3local44d7fcc9-gw1 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-01[default|t3]-local-44d7fcc9-gw1/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha01defaultt3local44d7fcc9-gw1_default  Creating
E                Network ubuntutestscha01defaultt3local44d7fcc9-gw1_default  Error
E               failed to create network ubuntutestscha01defaultt3local44d7fcc9-gw1_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-05[default|t3]]" time="127.218"><failure message="AssertionError: cluster ready">scenario = {'backend': 'default', 'faults': [{'kind': 'stop_cont', True: ['keeper2'], 'count': 1, 'sleep_s': 2.0}, {'kind': 'stop...ms': 6000, 'type': 'p99_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-05[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xe27f503feac0&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-05[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:97: in _make
    wait_until(lambda: count_leaders(nodes) == 1, timeout_s=to, interval=0.5, desc="cluster ready")
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

cond = &lt;function cluster_factory.&lt;locals&gt;._make.&lt;locals&gt;.&lt;lambda&gt; at 0xe27f55def7e0&gt;
timeout_s = 120.0, interval = 0.5, desc = 'cluster ready'

    def wait_until(cond, timeout_s=60.0, interval=0.5, desc=""):
        start = time.time()
        while True:
            try:
                if cond():
                    return True
            except Exception:
                pass
            if time.time() - start &gt; float(timeout_s):
&gt;               raise AssertionError(desc or "timeout")
E               AssertionError: cluster ready

tests/stress/keeper/framework/core/util.py:56: AssertionError</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[LDR-02[default|t3]]" time="126.521"><failure message="AssertionError: cluster ready">scenario = {'backend': 'default', 'faults': [{'kind': 'partition_oneway', True: 'leader', 'duration_s': 30}], 'gates': [{'type': ...'}, {'max_ratio': 0.1, 'type': 'error_rate_le'}, {'max_ms': 10000, 'type': 'p99_le'}], 'id': 'LDR-02[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xeb964ddbea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[LDR-02[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:97: in _make
    wait_until(lambda: count_leaders(nodes) == 1, timeout_s=to, interval=0.5, desc="cluster ready")
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

cond = &lt;function cluster_factory.&lt;locals&gt;._make.&lt;locals&gt;.&lt;lambda&gt; at 0xeb965378f7e0&gt;
timeout_s = 120.0, interval = 0.5, desc = 'cluster ready'

    def wait_until(cond, timeout_s=60.0, interval=0.5, desc=""):
        start = time.time()
        while True:
            try:
                if cond():
                    return True
            except Exception:
                pass
            if time.time() - start &gt; float(timeout_s):
&gt;               raise AssertionError(desc or "timeout")
E               AssertionError: cluster ready

tests/stress/keeper/framework/core/util.py:56: AssertionError</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[LDR-02[rocksdb|t3]]" time="3.419"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/.env --project-name ubuntutestsldr02rocksdbt3local44c6a56f-gw3 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestsldr02rocksdbt3local44c6a56f-gw3_default  Creating&#10; Network ubuntutestsldr02rocksdbt3local44c6a56f-gw3_default  Error&#10;failed to create network ubuntutestsldr02rocksdbt3local44c6a56f-gw3_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'partition_oneway', True: 'leader', 'duration_s': 30}], 'gates': [{'type': ...'}, {'max_ratio': 0.1, 'type': 'error_rate_le'}, {'max_ms': 10000, 'type': 'p99_le'}], 'id': 'LDR-02[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xfce59633ea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[LDR-02[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/.env', '--project-name', 'ubuntutestsldr02rocksdbt3local44c6a56f-gw3', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/.env --project-name ubuntutestsldr02rocksdbt3local44c6a56f-gw3 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-LDR-02[rocksdb|t3]-local-44c6a56f-gw3/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestsldr02rocksdbt3local44c6a56f-gw3_default  Creating
E                Network ubuntutestsldr02rocksdbt3local44c6a56f-gw3_default  Error
E               failed to create network ubuntutestsldr02rocksdbt3local44c6a56f-gw3_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[RCFG-06[default|t3]]" time="3.438"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/.env --project-name ubuntutestsrcfg06defaultt3local304f5705-gw5 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestsrcfg06defaultt3local304f5705-gw5_default  Creating&#10; Network ubuntutestsrcfg06defaultt3local304f5705-gw5_default  Error&#10;failed to create network ubuntutestsrcfg06defaultt3local304f5705-gw5_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'default', 'faults': [{'kind': 'partition_symmetric_during', 'on': 'leader', 'steps': [{'kind': 'reconfig'...nfig_members_len_eq'}, {'type': 'backlog_drains'}, {'type': 'health_precheck'}, ...], 'id': 'RCFG-06[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xf6f379b5ea20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[RCFG-06[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/.env', '--project-name', 'ubuntutestsrcfg06defaultt3local304f5705-gw5', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/.env --project-name ubuntutestsrcfg06defaultt3local304f5705-gw5 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-RCFG-06[default|t3]-local-304f5705-gw5/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestsrcfg06defaultt3local304f5705-gw5_default  Creating
E                Network ubuntutestsrcfg06defaultt3local304f5705-gw5_default  Error
E               failed to create network ubuntutestsrcfg06defaultt3local304f5705-gw5_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-05[rocksdb|t3]]" time="3.437"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/.env --project-name ubuntutestscha05rocksdbt3locala46153cb-gw13 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha05rocksdbt3locala46153cb-gw13_default  Creating&#10; Network ubuntutestscha05rocksdbt3locala46153cb-gw13_default  Error&#10;failed to create network ubuntutestscha05rocksdbt3locala46153cb-gw13_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'rocksdb', 'faults': [{'kind': 'stop_cont', True: ['keeper2'], 'count': 1, 'sleep_s': 2.0}, {'kind': 'stop...ms': 6000, 'type': 'p99_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-05[rocksdb|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xe60a721d2ac0&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-05[rocksdb|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/.env', '--project-name', 'ubuntutestscha05rocksdbt3locala46153cb-gw13', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/.env --project-name ubuntutestscha05rocksdbt3locala46153cb-gw13 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-05[rocksdb|t3]-local-a46153cb-gw13/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha05rocksdbt3locala46153cb-gw13_default  Creating
E                Network ubuntutestscha05rocksdbt3locala46153cb-gw13_default  Error
E               failed to create network ubuntutestscha05rocksdbt3locala46153cb-gw13_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase><testcase classname="tests.stress.keeper.tests.test_scenarios" name="test_scenario[CHA-04[default|t3]]" time="3.647"><failure message="Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/.env --project-name ubuntutestscha04defaultt3local9e56b626-gw10 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha04defaultt3local9e56b626-gw10_default  Creating&#10; Network ubuntutestscha04defaultt3local9e56b626-gw10_default  Error&#10;failed to create network ubuntutestscha04defaultt3local9e56b626-gw10_default: Error response from daemon: all predefined address pools have been fully subnetted">scenario = {'backend': 'default', 'faults': [{'kind': 'netem', True: 'all', 'loss_pct': 2, 'jitter_ms': 5, ...}], 'gates': [{'typ...ms': 4000, 'type': 'p95_le'}, {'type': 'log_sanity_ok'}, {'type': 'health_precheck'}], 'id': 'CHA-04[default|t3]', ...}
cluster_factory = &lt;function cluster_factory.&lt;locals&gt;._make at 0xee4be33caa20&gt;
request = &lt;FixtureRequest for &lt;Function test_scenario[CHA-04[default|t3]]&gt;&gt;
run_meta = {'backend': 'default', 'commit_sha': 'local'}

    @pytest.mark.timeout(2400)
    def test_scenario(scenario, cluster_factory, request, run_meta):
        start_ts = time.time()
        topo=scenario.get("topology",3)
        flags=scenario.get("keeper_flags_xml","")
        backend = scenario.get("backend") or request.config.getoption("--keeper-backend")
        # Effective run_meta per test with the scenario-selected backend
        run_meta_eff = dict(run_meta or {})
        try:
            run_meta_eff["backend"] = backend
        except Exception:
            pass
        opts=scenario.get("opts", {})
        try:
            faults_mode = request.config.getoption("--faults")
        except Exception:
            faults_mode = os.environ.get("KEEPER_FAULTS", "on")
        try:
            rnd_count = int(request.config.getoption("--random-faults-count") or 1)
        except Exception:
            rnd_count = int(os.environ.get("KEEPER_RANDOM_FAULTS_COUNT", "1"))
        try:
            seed_val = int(request.config.getoption("--seed") or 0)
        except Exception:
            seed_val = 0
        fs_original = scenario.get("faults", []) or []
        fs_effective = fs_original
        if faults_mode == "off":
            fs_effective = []
        elif faults_mode == "random":
            try:
                dur_default = int((scenario.get("workload") or {}).get("duration") or request.config.getoption("--duration"))
            except Exception:
                dur_default = int(os.environ.get("KEEPER_DURATION", "120"))
            if seed_val &lt;= 0:
                import os as _os
                seed_val = int.from_bytes(_os.urandom(4), 'big')
            try:
                try:
                    inc_raw = request.config.getoption("--random-faults-include") or os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                except Exception:
                    inc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_INCLUDE", "")
                try:
                    exc_raw = request.config.getoption("--random-faults-exclude") or os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                except Exception:
                    exc_raw = os.environ.get("KEEPER_RANDOM_FAULTS_EXCLUDE", "")
                inc = set([x.strip() for x in (inc_raw or "").split(",") if x.strip()])
                exc = set([x.strip() for x in (exc_raw or "").split(",") if x.strip()])
                cands = [k for k in list(getattr(fault_registry, 'keys', lambda: [])()) if k not in _FUZZ_EXCLUDE]
                if not cands and isinstance(fault_registry, dict):
                    cands = [k for k in fault_registry.keys() if k not in _FUZZ_EXCLUDE]
                weights = {}
                if inc:
                    for k in cands:
                        weights[k] = 1 if k in inc else 0
                else:
                    for k in cands:
                        weights[k] = 0 if k in exc else 1
                fz = generate_fuzz_scenario(seed_val, max(1, rnd_count), dur_default, weights=weights)
                rnd_faults = fz.get("faults", []) or []
                rb = {"kind": "run_bench", "duration_s": dur_default}
                fs_effective = [{"kind": "parallel", "steps": [rb] + rnd_faults}]
            except Exception:
                fs_effective = fs_original
        try:
            uses_dm = any((f.get("kind") in ("dm_delay","dm_error")) for f in fs_effective)
            if uses_dm:
                os.environ.setdefault("KEEPER_PRIVILEGED", "1")
                os.environ.setdefault("KEEPER_HOST_AUTO_PROVISION", "1")
            # Relax log gate for known transient errors during chaos if not provided by env
            os.environ.setdefault(
                "KEEPER_LOG_ALLOW",
                "failed to read rpc header.*End of file|DNS error|Not found address of host|Net Exception: Socket is not connected|CANNOT_READ_ALL_DATA|Cannot read all data|Unknown table.*system\\.trace_log|Unknown table expression identifier 'system\\.trace_log'"
            )
        except Exception:
            pass
        # compute run_id early for reproducible artifact paths
        run_id=f"{scenario.get('id','')}-{run_meta.get('commit_sha','local')}-{uuid.uuid4().hex[:8]}"
        # Use a unique cluster name per test to avoid instance-dir collisions
        try:
            cname = run_id.replace('/', '_').replace(' ', '_')
            os.environ["KEEPER_CLUSTER_NAME"] = cname
        except Exception:
            pass
&gt;       cluster, nodes = cluster_factory(topo, backend, opts)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

tests/stress/keeper/tests/test_scenarios.py:215: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
tests/stress/keeper/tests/conftest.py:86: in _make
    cluster, nodes = builder.build(topology=topology, backend=backend, opts=opts)
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
tests/stress/keeper/framework/core/cluster.py:200: in build
    cluster.start()
tests/integration/helpers/cluster.py:3728: in start
    run_and_check(clickhouse_start_cmd)
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

args = ['docker', 'compose', '--env-file', '/home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/.env', '--project-name', 'ubuntutestscha04defaultt3local9e56b626-gw10', ...]
env = None, shell = False, stdout = -1, stderr = -1, timeout = 300
nothrow = False, detach = False

    def run_and_check(
        args: Union[Sequence[str], str],
        env=None,
        shell=False,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        timeout=300,
        nothrow=False,
        detach=False,
    ) -&gt; str:
        if shell:
            if isinstance(args, str):
                shell_args = args
            else:
                shell_args = next(a for a in args)
        else:
            shell_args = " ".join(args)
    
        logging.debug("Command:[%s]", shell_args)
        if detach:
            subprocess.Popen(
                args,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env=env,
                shell=shell,
            )
            return ""
    
        res = subprocess.run(
            args,
            stdout=stdout,
            stderr=stderr,
            env=env,
            shell=shell,
            timeout=timeout,
            check=False,
        )
        out = res.stdout.decode("utf-8", "ignore")
        err = res.stderr.decode("utf-8", "ignore")
        # check_call(...) from subprocess does not print stderr, so we do it manually
        for outline in out.splitlines():
            logging.debug("Stdout:%s", outline)
        for errline in err.splitlines():
            logging.debug("Stderr:%s", errline)
        if res.returncode != 0:
            logging.debug("Exitcode:%s", res.returncode)
            if env:
                logging.debug("Env:%s", env)
            if not nothrow:
&gt;               raise Exception(
                    f"Command [{shell_args}] return non-zero code {res.returncode}: {res.stderr.decode('utf-8')}"
                )
E               Exception: Command [docker compose --env-file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/.env --project-name ubuntutestscha04defaultt3local9e56b626-gw10 --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/keeper1/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/keeper2/docker-compose.yml --file /home/ubuntu/ClickHouse/tests/stress/keeper/tests/_instances-CHA-04[default|t3]-local-9e56b626-gw10/keeper3/docker-compose.yml up -d --no-recreate] return non-zero code 1:  Network ubuntutestscha04defaultt3local9e56b626-gw10_default  Creating
E                Network ubuntutestscha04defaultt3local9e56b626-gw10_default  Error
E               failed to create network ubuntutestscha04defaultt3local9e56b626-gw10_default: Error response from daemon: all predefined address pools have been fully subnetted

tests/integration/helpers/cluster.py:191: Exception</failure></testcase></testsuite></testsuites>