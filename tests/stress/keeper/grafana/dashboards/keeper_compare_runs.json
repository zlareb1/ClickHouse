{
    "__inputs": [
        {
            "name": "DS_CLICKHOUSE",
            "label": "ClickHouse",
            "description": "ClickHouse datasource",
            "type": "datasource",
            "pluginId": "grafana-clickhouse-datasource",
            "pluginName": "ClickHouse"
        }
    ],
    "__requires": [
        {
            "type": "grafana",
            "id": "grafana",
            "name": "Grafana",
            "version": "9.5.0"
        },
        {
            "type": "datasource",
            "id": "grafana-clickhouse-datasource",
            "name": "ClickHouse",
            "version": "3.7.0"
        }
    ],
    "title": "Keeper Compare Runs (default.checks)",
    "editable": true,
    "panels": [
        {
            "type": "table",
            "title": "A/B Bench Delta",
            "datasource": {
                "type": "grafana-clickhouse-datasource",
                "uid": "${DS_CLICKHOUSE}"
            },
            "gridPos": {
                "h": 10,
                "w": 24,
                "x": 0,
                "y": 0
            },
            "options": {
                "showHeader": true
            },
            "targets": [
                {
                    "format": 1,
                    "query": "WITH A AS (SELECT JSONExtractString(test_context_raw,'scenario') AS scenario, JSONExtractString(test_context_raw,'backend') AS backend, JSONExtractFloat(test_context_raw,'p95_ms') AS p95_ms, JSONExtractFloat(test_context_raw,'p99_ms') AS p99_ms, JSONExtractFloat(test_context_raw,'rps') AS rps, JSONExtractFloat(test_context_raw,'error_rate') AS error_rate FROM default.checks WHERE check_name='keeper-stress' AND JSONExtractString(test_context_raw,'run_id')='${run_a}'), B AS (SELECT JSONExtractString(test_context_raw,'scenario') AS scenario, JSONExtractString(test_context_raw,'backend') AS backend, JSONExtractFloat(test_context_raw,'p95_ms') AS p95_ms, JSONExtractFloat(test_context_raw,'p99_ms') AS p99_ms, JSONExtractFloat(test_context_raw,'rps') AS rps, JSONExtractFloat(test_context_raw,'error_rate') AS error_rate FROM default.checks WHERE check_name='keeper-stress' AND JSONExtractString(test_context_raw,'run_id')='${run_b}') SELECT coalesce(A.scenario,B.scenario) AS scenario, coalesce(A.backend,B.backend) AS backend, A.rps AS rps_a, B.rps AS rps_b, (B.rps - A.rps) AS delta_rps, A.p95_ms AS p95_a, B.p95_ms AS p95_b, (B.p95_ms - A.p95_ms) AS delta_p95, A.p99_ms AS p99_a, B.p99_ms AS p99_b, (B.p99_ms - A.p99_ms) AS delta_p99, A.error_rate AS err_a, B.error_rate AS err_b, (B.error_rate - A.error_rate) AS delta_err FROM A FULL OUTER JOIN B USING (scenario, backend) ORDER BY scenario, backend"
                }
            ]
        }
    ],
    "templating": {
        "list": [
            {
                "name": "run_a",
                "type": "query",
                "label": "Run A (baseline)",
                "datasource": {
                    "type": "grafana-clickhouse-datasource",
                    "uid": "${DS_CLICKHOUSE}"
                },
                "query": "SELECT DISTINCT JSONExtractString(test_context_raw,'run_id') AS run_id FROM default.checks WHERE check_name='keeper-stress' ORDER BY run_id DESC LIMIT 200",
                "refresh": 1
            },
            {
                "name": "run_b",
                "type": "query",
                "label": "Run B (candidate)",
                "datasource": {
                    "type": "grafana-clickhouse-datasource",
                    "uid": "${DS_CLICKHOUSE}"
                },
                "query": "SELECT DISTINCT JSONExtractString(test_context_raw,'run_id') AS run_id FROM default.checks WHERE check_name='keeper-stress' ORDER BY run_id DESC LIMIT 200",
                "refresh": 1
            }
        ]
    },
    "schemaVersion": 37,
    "version": 1
}